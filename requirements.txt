Django>=5.0,<5.2
djangorestframework>=3.15
djangorestframework-simplejwt>=5.3
django-cors-headers>=4.3
python-dotenv>=1.0
gunicorn>=22.0
# IMPORTANT: transformers 5.x needs torch >= 2.5.
# Install the CUDA wheel explicitly after pip install -r requirements.txt:
#   pip install 'torch>=2.5.1,<2.7' --index-url https://download.pytorch.org/whl/cu121
torch>=2.5.1,<2.7
transformers>=5.0,<6
accelerate>=0.33
sentencepiece>=0.2
huggingface_hub>=0.24
protobuf>=4.25
safetensors>=0.4
